enviro_name "Dawn1 - Basic Dawn Env"

timeofday "Dusk"

water_rgb         52,62,74      ;color seen when under water and surface color

sky_map1          dusk1.pcx     ;cloud pcx, shadow & reflect layer
sky_map2          dusk1b.pcx    ;cloud pcx, wispy lower layer
sky_height        160            ;in meters
sky_speed         25             ;movement rate

fog_level         1000           ;0=1000 meters 1=500 meters etc.
fog_type          3             ;0=fog, 1=haze, 2=haze wall, 3=mild haze wall

sun_rgb           70,52,48      ;at 50 degrees west
sky_rgb           50,46,59      ;overhead and wraps around
ground_rgb        20,20,27      ;underneath, wraps around
fog_rgb           90,69,64      ;fog color
skyfog_rgb        91,72,69      ;horizon fog color 

cloud_rgb         128,128,128   ;cloud color tinter
terrain_rgb       255,255,255   ;1.0 terrain tinting color (255,255,255 is default)
vertex_rgb        54,54,54      ;2.0 vertex tinting color (128,128,128 is default)

lightning_rgb     85,85,96      ;lightning color (from sky direction)
				;lightning is added to sky_rgb
				;and also affects fog and ambient slightly

sun_3di                         ;sun/moon object
glare_3di                       ;glare object
star_3di                        ;star object / leave blank for null

;indoor ambient lighting values
ceiling_rgb     22,22,22        ;overhead and wraps around
floor_rgb       10,10,10        ;underneath, wraps around
